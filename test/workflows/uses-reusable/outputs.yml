name: reusable workflow output test

on: 
  push:
    branches:
      - master
  workflow_call:

permissions:
  contents: read

jobs:
  outputs-reusable:
    uses: ./test/workflows/workflow_call/outputs.yml

  output-test:
    runs-on: ubuntu-latest
    needs: outputs-reusable
    steps:
    - name: echo outputs
      run: |
        echo '${{ needs.outputs-reusable.outputs }}'
