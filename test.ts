import prompts, { PromptObject, Choice } from 'prompts';

// const res = await prompts({
//   type: 'text',
//   message: 'dddd',
//   name: 'test',
// });

const ee = await prompts({

});

console.log('ee', ee);
